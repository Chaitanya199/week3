import math

class Rectangle ():
	def __init__ (self, c):
		self.width = 100.0
		self.height = 200.0
		self.corner = c
		self.corner = 0.0
		self.corner = 0.0
	def find_center (self):
		c.x = self.corner.x + self.width/2.0
		c.y = self.corner.y + self.height/2.0
		

center = Rectangle(50)
print (str(x.find_center(y)))
